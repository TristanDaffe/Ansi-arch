---
- name: install rofi
  pacman:
    name: rofi
    state: present
  become: true

- name: Copy the config folder
  ansible.builtin.copy:
    src: "templates/config/rofi/"
    dest: "{{ ansible_env.HOME }}/.config/rofi/"

